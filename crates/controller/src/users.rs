use models::user::User;

pub struct Users;

// #[async_trait]
impl Users {
    // pub fn update_password_hash(user: &User) {
    //     sqlx::query("UPDATE users SET password_hash = ? WHERE id = ?")
    //         .bind(&user.password_hash)
    //         .bind(&user.id)
    //         .fetch();
    // }
}
